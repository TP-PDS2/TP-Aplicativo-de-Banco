#ifndef SALDO_HPP
#define SALDO_HPP

class Saldo {
private:
    double saldo;

public:
    // Getter para saldo
    double getSaldo() const;

    // Setter para saldo
    void setSaldo(double novoSaldo);
};

#endif
